export interface Survey {
}

export interface SurveyData {
  id: number;
  title: String;
  target: number;
  createdAt: String;
  endDate: String;
}

export interface SimpleSurveyData {
  id: number;
  title: String;
}