import { RespondentListState } from './respondent-list-state';

describe('RespondentListState', () => {
  it('should create an instance', () => {
    expect(new RespondentListState()).toBeTruthy();
  });
});
